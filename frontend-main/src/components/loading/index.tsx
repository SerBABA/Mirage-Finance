import { IconWrapper, Wrapper, Title, Icon } from './loading.elements';

export const Loading = () => (
  <Wrapper>
    <IconWrapper size={90}>
      <Icon></Icon>
    </IconWrapper>
    <Title>LOADING</Title>
  </Wrapper>
);
